global {
    p99-response-time-in-millis=2000

    p99-response-time-in-millis=${?P99_RESPONSE_TIME_IN_MILLIS}
    successful-requests-percentage=99
    successful-requests-percentage=${?SUCCESSFUL_REQUESTS_PERCENTAGE}
    target-requests-per-second-percentage=95
    target-requests-per-second-percentage=${?TARGET_REQUESTS_PER_SECOND_PERCENTAGE}
}